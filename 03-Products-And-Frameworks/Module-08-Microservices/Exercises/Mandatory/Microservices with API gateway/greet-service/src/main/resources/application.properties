spring.application.name=greet-service

# greet-service runs on port 9091 - must not clash with account(8080), loan(8081), eureka(8761), api-gateway(9090)
server.port=9091

# tells this service where to find the eureka discovery server
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
